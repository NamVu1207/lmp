import { Button } from "antd";

const MButton = ({ row }) => {
  return (
    <Button
      style={{ width: "100%" }}
      onClick={() => {
      }}
    >
      Click
    </Button>
  );
};

export default MButton;
